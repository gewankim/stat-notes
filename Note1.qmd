---
title: "SOU Math Department Stat 243Z: Elementary Statistics"
author: "Professor Daniel Kim, Ph.D."
format:
  html:
    theme: cosmo
    toc: true
    number-sections: true
    code-fold: show
    math: mathjax
---

# Lecture 1: Introduction

Welcome to Stat 243Z.

## Course Info
- Instructor: **Dr. Daniel Kim**
- Title: *Elementary Statistics*

## A bit of math
The standard normal CDF is \( \Phi(z) = P(Z \le z) \).

## A tiny Python example (renders a plot)
```{python}
import numpy as np, matplotlib.pyplot as plt
from scipy.stats import norm

x = np.linspace(-5,5,401)
y = norm.pdf(x)

# Plot the normal curve and shade between -2 and 2
plt.figure()
plt.plot(x, y, color="black")
plt.fill_between(x, 0, y, where=(x>=-2) & (x<=2), color="red", alpha=0.35)
plt.xlim(-4, 4); plt.ylim(0, 0.5)
plt.title("Standard Normal PDF"); plt.xlabel("z"); plt.ylabel("density")
plt.tight_layout()
plt.show()
